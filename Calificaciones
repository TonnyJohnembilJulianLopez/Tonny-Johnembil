#include <stdio.h>
#include <stdlib.h>

int main()
{
    float calf,prom;
    printf("\nIngresa tu calificacion:" ,calf);
    scanf("%f" ,&calf);
    prom=calf;

    if(prom>=7.9 && prom<=8.5)
        printf("\nSatisfactorio");

    else if(prom<7.8)
        printf("\nReprobado");

    if(prom>=8.6 && prom<=9.6)
        printf("\nDestacado");

    if(prom>=9.7 && prom<=10)
        printf("\nAutonomo");


    return 0;
}
